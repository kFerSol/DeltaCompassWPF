<UserControl x:Class="DeltaCompassWPF.Views.UserControls.BindablePasswordBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeltaCompassWPF.Views.UserControls"
             mc:Ignorable="d">
    <Border Background="#C2C2C2" BorderBrush="#2E2BC2" BorderThickness="1.5" CornerRadius="15" 
            VerticalAlignment="Center">
        <Grid>
            <Label x:Name="placeSenha" Content="{Binding Placeholder, RelativeSource={RelativeSource AncestorType=UserControl}}" FontSize="16" FontFamily="../../fonts/#Poppins" 
                   Foreground="#888888" Margin="5, 0, 5, 0"  Padding="2, 5, 0, 5" VerticalAlignment="Center"/>
            <PasswordBox x:Name="txtSenha"  FontSize="16" BorderThickness="0" Margin="5, 0, 5, 0" 
                         Background="Transparent" FontFamily="../../fonts/#Poppins" Padding="0, 5, 0, 5" 
                         PasswordChanged="txtSenha_PasswordChanged"/>
        </Grid>
    </Border>
</UserControl>
